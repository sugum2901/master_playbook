---
- name: creating variables
  hosts: dev
  tasks:
    - name: creating a directory
      ansible.builtin.file:
        path: "{{DIRECTORY}}"
        state: directory
    - name: creating a file
      ansible.builtin.file:
        path: "{{FILE}}"
        state: touch
    - name: copy the content to the index.html
      ansible.builtin.copy:
        content: "{{CONTENT}}"
        dest: "{{DIRECTORY}}/{{FILE}}"
